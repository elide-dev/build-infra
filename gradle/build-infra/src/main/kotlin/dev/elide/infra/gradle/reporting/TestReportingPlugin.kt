package dev.elide.infra.gradle.reporting

import org.gradle.api.Plugin
import org.gradle.api.Project

/**
 * # Reporting: Aggregated Tests
 */
public class TestReportingPlugin : Plugin<Project> {
  override fun apply(target: Project) {
    //
  }
}
