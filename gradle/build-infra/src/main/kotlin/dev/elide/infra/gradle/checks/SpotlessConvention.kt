package dev.elide.infra.gradle.checks

import org.gradle.api.Plugin
import org.gradle.api.Project

/**
 * # Checks: Spotless
 */
public class SpotlessConvention : Plugin<Project> {
  override fun apply(target: Project) {
    //
  }
}
