name: "Build: Gradle (JVM)"

on:
  workflow_call: {}
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
    hello:
        name: "Test: Say Hello"
        runs-on: ubuntu-latest
        steps:
          - name: Harden Runner
            uses: step-security/harden-runner@1b05615854632b887b69ae1be8cbefe72d3ae423 # v2.6.0
            with:
              egress-policy: audit

          - name: "Emit Message"
            run: echo "Hello from reusable actions"
