
## elide ci: actions

This directory defines custom action steps for use with GitHub Actions. Each step subdirectory defines a single
action, with embedded dependencies, configuration, and so on.

More coming soon; no actions are defined yet.
